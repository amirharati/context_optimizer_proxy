# --- Secrets & local env (never commit real keys)
.env
.env.local
.env.*.local
.envrc
!.env.example

# --- Runtime logs / session dumps (large, sensitive, regenerable)
# Covers: logs/sessions/*.jsonl, token_savings.jsonl, debug_buffer.json,
#         last_request.json, cursor_tools_dump.json, fetches/, etc.
logs/
runs/

# --- Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# --- Virtual environments
venv/
.venv/
ENV/
env/

# --- Tests / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage*
htmlcov/
.tox/
.nox/
.cache/

# --- OS junk
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# --- Local handoff / scratch (TEMP_*.md — not committed; see docs/README.md)
docs/temp/
